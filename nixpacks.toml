# Monorepo: Root Directory = apps/web olsa bile kurulum kökten yapılır.

[phases.install]
cmds = ["cd ../.. && npm install"]

[phases.build]
cmds = ["cd ../.. && npm run build:web"]

[start]
cmd = "npm run start"
